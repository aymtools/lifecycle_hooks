name: an_lifecycle_hooks
description: "A middleware for connecting flutter_hooks and anlifecycle."
version: 1.0.3
homepage: https://github.com/aymtools/lifecycle_hooks/
issue_tracker: https://github.com/aymtools/lifecycle_hooks/issues

topics:
  - hooks-lifecycle
  - use-lifecycle
  - anlifecycle
  - use-lifecycle-view-model

environment:
  sdk: '>=2.17.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

  weak_collections: ^1.1.0
  flutter_hooks: ^0.18.4

  cancellable: ^2.0.1
  anlifecycle: ^3.0.2
  an_lifecycle_cancellable: ^1.0.1
  an_lifecycle_viewmodel: ^1.0.1

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
